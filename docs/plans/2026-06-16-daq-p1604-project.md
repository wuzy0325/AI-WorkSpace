# DAQ-P-1604 压力采集桌面应用 实施计划

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 基于 daq-t1603 项目架构，创建 daq-p-1604 压力采集桌面应用，架构完全一致，硬件层替换为 DAQ-P-1604 压力设备驱动。

**Architecture:** 采用与 daq-t1603 完全一致的单模块 Wails 布局（`apps/desktop-wails/{core,ports,usecase,adapters,backend,frontend}`），遵循六边形架构边界。硬件适配器使用 `shared/device-sdk/go/protocol` 中的 `FrameReader`、`ParseStreamFrame`、`IsASCIIFrame` 解析 P1604 二进制协议，设备发现使用 UDP 广播 `psi9000` 命令。核心差异：18 通道（16 压力 + 大气压力 + 大气温度）、TCP 二进制协议、压力单位系统（psi/Pa/kPa/MPa/kgf/cm²）。

**Tech Stack:** Go 1.25 + Wails v2 + Vue 3 + Pinia + Naive UI + ECharts + shared/device-sdk/go

---

## 关键差异对照：T1603 vs P1604

| 维度 | DAQ-T-1603 | DAQ-P-1604 |
|------|-----------|-----------|
| 通道数 | 16（热电偶温度） | 18（16 压力 + CH17 大气压力 + CH18 大气温度） |
| 默认单位 | °C | 由 EU 系数决定（psi/Pa/kPa/MPa/kgf/cm²） |
| 默认精度 | 2 位小数 | 压力 3 位、大气压 0 位、温度 2 位 |
| 通信协议 | TCP ASCII | TCP 二进制（2 字节大端长度前缀） |
| 连接初始化 | 无特殊 | 必须先发 `w1601` 启用长度前缀 |
| 采集命令 | SCPI 风格 | `c 00`/`c 05`/`c 01`/`c 02` |
| 数据帧 | ASCII 文本 | 二进制帧（5 字节头 + 18×float32 BE，前 16 通道逆序） |
| 设备发现 | UDP `T1603` → 7000 | UDP `psi9000` → 7000，监听 7001 |
| 默认地址 | 192.168.x.x:9000 | 192.168.3.101:9000 |
| 特有功能 | 热电偶类型配置 | 阀门控制、EU 系数/单位系统、校准 |
| 设备特有配置 | T1603Config（热电偶类型、通道掩码等） | P1604Config（采样周期、单位、阀门状态等） |

---

## Task 1: 创建项目骨架和 Go 模块

**Files:**
- Create: `projects/daq-p1604/VERSION`
- Create: `projects/daq-p1604/apps/desktop-wails/go.mod`
- Create: `projects/daq-p1604/apps/desktop-wails/main.go`
- Create: `projects/daq-p1604/apps/desktop-wails/wails.json`

**Step 1: 创建项目目录结构**

```powershell
mkdir -p projects/daq-p1604/apps/desktop-wails/{core,ports,usecase,backend,adapters/{hardware,config,recording,logging},frontend,cmd}
```

**Step 2: 创建 VERSION 文件**

```
0.1.0
```

**Step 3: 创建 go.mod**

```go
module daq-p1604

go 1.25.0

require (
	github.com/wailsapp/wails/v2 v2.12.0
	shared.local/device-sdk/go v0.0.0-00010101000000-000000000000
)

// ... indirect deps 与 daq-t1603 相同

replace shared.local/device-sdk/go => ../../../../shared/device-sdk/go
```

**Step 4: 创建 wails.json**

```json
{
  "$schema": "https://wails.io/schemas/config.v2.json",
  "name": "daq-p1604",
  "outputfilename": "DAQ-P-1604",
  "frontend:dir": "frontend",
  "frontend:install": "npm install --no-audit --no-fund",
  "frontend:build": "npm run build",
  "frontend:dev:watcher": "npm run dev",
  "frontend:dev:serverUrl": "http://localhost:15175",
  "author": {
    "name": "DAQ-P-1604 Team",
    "email": ""
  }
}
```

**Step 5: 创建 main.go（骨架，后续 Task 完善）**

```go
package main

import (
	"embed"
	"log"
	"os"
	"path/filepath"

	"github.com/wailsapp/wails/v2"
	"github.com/wailsapp/wails/v2/pkg/options"
	"github.com/wailsapp/wails/v2/pkg/options/assetserver"

	"daq-p1604/adapters/config"
	"daq-p1604/adapters/hardware"
	"daq-p1604/adapters/logging"
	"daq-p1604/adapters/recording"
	"daq-p1604/backend"
	"daq-p1604/ports"
	"daq-p1604/usecase"
)

//go:embed all:frontend/dist
var assets embed.FS

func main() {
	configDir, _ := os.UserConfigDir()
	if configDir != "" {
		configDir = filepath.Join(configDir, "daq-p1604")
	} else {
		configDir = "config"
	}
	os.MkdirAll(configDir, 0755)
	cfgStore := config.NewJSONConfigStore(filepath.Join(configDir, "device-profiles.json"))

	logDir := filepath.Join(configDir, "logs")
	os.MkdirAll(logDir, 0755)

	var devAdapter ports.DevicePort
	var scanner ports.DeviceScanPort
	var logCapableAdapter interface {
		SetLogSink(func(hardware.DeviceLogEntry))
	}
	if os.Getenv("DAQ_P1604_MODE") == "simulated" {
		devAdapter = hardware.NewSimulatedAdapter()
		scanner = hardware.NewSimulatedScanner()
	} else {
		p1604Adapter := hardware.NewP1604Adapter()
		devAdapter = p1604Adapter
		logCapableAdapter = p1604Adapter
		scanner = hardware.NewP1604Scanner()
	}
	recorder := recording.NewCSVRecorder()
	logWriter := logging.NewLogFileWriter()

	deviceUC := usecase.NewDeviceUsecase(devAdapter, cfgStore, scanner)
	recordUC := usecase.NewRecordingUsecase(recorder)
	logUC := usecase.NewLogUsecase(logWriter)

	app := backend.NewApp(deviceUC, recordUC, logUC, logDir)
	if logCapableAdapter != nil {
		logCapableAdapter.SetLogSink(func(entry hardware.DeviceLogEntry) {
			app.EmitLog(backend.LogEvent{
				Level:    entry.Level,
				Category: entry.Category,
				DeviceID: entry.DeviceID,
				Source:   "hardware",
				Message:  entry.Message,
				Detail:   entry.Detail,
			})
		})
	}

	err := wails.Run(&options.App{
		Title:     "DAQ-P-1604 压力采集",
		Width:     1600,
		Height:    900,
		MinWidth:  1280,
		MinHeight: 720,
		AssetServer: &assetserver.Options{
			Assets: assets,
		},
		OnStartup:  app.Startup,
		OnShutdown: app.Shutdown,
		Bind: []interface{}{
			app,
		},
	})
	if err != nil {
		log.Fatal(err)
	}
}
```

**Step 6: 验证 Go 模块初始化**

```powershell
cd projects\daq-p1604\apps\desktop-wails
go mod tidy
```

---

## Task 2: Core 层 — 类型定义

**Files:**
- Create: `projects/daq-p1604/apps/desktop-wails/core/types.go`
- Create: `projects/daq-p1604/apps/desktop-wails/core/recording.go`

**Step 1: 创建 core/types.go**

与 T1603 的 `core/types.go` 结构一致，但将温度相关类型替换为压力相关：

- `T1603Config` → `P1604Config`：包含采样周期(ms)、单位(psi/Pa/kPa/MPa/kgf/cm²)、自动连接等
- `TemperatureProfile` → `PressureProfile`：包含 P1604Cfg
- `TemperatureSnapshot` → `PressureSnapshot`：18 通道数据
- `ChannelConfig`：保留，增加压力相关字段
- `DeviceState`：Profile 类型改为 PressureProfile
- `ScanResult`：ID 前缀改为 `scan-daq-p-1604`

```go
package core

import "time"

type DeviceStatus int

const (
	StatusDisconnected DeviceStatus = iota
	StatusConnected
	StatusAcquiring
	StatusError
)

func (s DeviceStatus) String() string {
	switch s {
	case StatusDisconnected:
		return "Disconnected"
	case StatusConnected:
		return "Connected"
	case StatusAcquiring:
		return "Acquiring"
	case StatusError:
		return "Error"
	default:
		return "Unknown"
	}
}

// P1604Config DAQ-P-1604 设备硬件配置
type P1604Config struct {
	SamplingRate int    `json:"samplingRate"` // 采样周期（毫秒），最小 10ms
	Unit         string `json:"unit"`         // 压力单位：psi, Pa, kPa, MPa, kgf/cm²
	AutoConnect  bool   `json:"autoConnect"`  // 启动时自动连接
}

// ChannelConfig 通道配置
type ChannelConfig struct {
	Index     int     `json:"index"`
	Name      string  `json:"name"`
	Enabled   bool    `json:"enabled"`
	Unit      string  `json:"unit"`
	Color     string  `json:"color"`
	Precision int     `json:"precision"`
	RangeMin  float64 `json:"rangeMin,omitempty"`
	RangeMax  float64 `json:"rangeMax,omitempty"`
}

// PressureProfile 压力采集设备配置档案
type PressureProfile struct {
	ID           string          `json:"id"`
	Name         string          `json:"name"`
	Address      string          `json:"address"`
	Port         int             `json:"port"`
	SamplingRate int             `json:"samplingRate"`
	Channels     []ChannelConfig `json:"channels"`
	P1604Cfg     P1604Config     `json:"p1604Config"`
	CreatedAt    int64           `json:"createdAt,omitempty"`
}

// PressureSnapshot 压力采集数据快照
type PressureSnapshot struct {
	DeviceID          string    `json:"deviceId"`
	Timestamp         int64     `json:"timestamp"`
	HardwareTimestamp float64   `json:"hardwareTimestamp,omitempty"`
	Values            []float64 `json:"values"` // 18 通道：CH1-CH16 压力 + CH17 大气压力 + CH18 大气温度
	Unit              string    `json:"unit"`
}

// DeviceState 设备运行状态
type DeviceState struct {
	Profile      PressureProfile `json:"profile"`
	Status       DeviceStatus    `json:"status"`
	StatusText   string          `json:"statusText"`
	Error        string          `json:"error"`
	ConnectedAt  int64           `json:"connectedAt"`
	AcquiringAt  int64           `json:"acquiringAt"`
	SamplingRate float64         `json:"samplingRate"`
}

// ScanResult 设备扫描结果
type ScanResult struct {
	ID              string `json:"id"`
	Name            string `json:"name"`
	Address         string `json:"address"`
	Port            int    `json:"port"`
	MacAddress      string `json:"macAddress,omitempty"`
	SerialNumber    string `json:"serialNumber,omitempty"`
	FirmwareVersion string `json:"firmwareVersion,omitempty"`
}

func (s *DeviceState) SetStatus(status DeviceStatus) {
	s.Status = status
	s.StatusText = status.String()
}

func TimestampMs() int64 {
	return time.Now().UnixMilli()
}
```

**Step 2: 创建 core/recording.go**

与 T1603 完全一致（RecordingStatus、RecordingSession 不涉及设备特定逻辑）：

```go
package core

type RecordingStatus int

const (
	RecordingIdle RecordingStatus = iota
	RecordingActive
)

type RecordingSession struct {
	ID            string          `json:"id"`
	OutputDir     string          `json:"outputDir"`
	FilePrefix    string          `json:"filePrefix"`
	StartTimeMs   int64           `json:"startTimeMs"`
	SnapshotCount int             `json:"snapshotCount"`
	Status        RecordingStatus `json:"status"`
}
```

---

## Task 3: Ports 层 — 接口定义

**Files:**
- Create: `projects/daq-p1604/apps/desktop-wails/ports/device.go`
- Create: `projects/daq-p1604/apps/desktop-wails/ports/scan.go`
- Create: `projects/daq-p1604/apps/desktop-wails/ports/config.go`
- Create: `projects/daq-p1604/apps/desktop-wails/ports/recording.go`
- Create: `projects/daq-p1604/apps/desktop-wails/ports/logging.go`

**Step 1: 创建 ports/device.go**

```go
package ports

import "daq-p1604/core"

// DevicePort 设备操作端口接口
type DevicePort interface {
	Connect(profile core.PressureProfile) error
	Disconnect(id string) error
	StartAcquisition(id string) (<-chan core.PressureSnapshot, error)
	StopAcquisition(id string) error
	Status(id string) (core.DeviceState, bool)
	ApplyConfig(id string, cfg core.P1604Config) error
	SetDataSink(id string, sink func(core.PressureSnapshot))
}
```

**Step 2: 创建 ports/scan.go**

```go
package ports

import "daq-p1604/core"

// DeviceScanPort 设备扫描端口接口
type DeviceScanPort interface {
	Scan() ([]core.ScanResult, error)
}
```

**Step 3: 创建 ports/config.go**

```go
package ports

import "daq-p1604/core"

// ConfigPort 配置持久化端口接口
type ConfigPort interface {
	LoadProfiles() ([]core.PressureProfile, error)
	SaveProfile(profile core.PressureProfile) error
	DeleteProfile(id string) error
}
```

**Step 4: 创建 ports/recording.go**

```go
package ports

import "daq-p1604/core"

// RecordingPort 数据录制端口接口
type RecordingPort interface {
	Start(outputDir string, prefix string) error
	Write(snapshot core.PressureSnapshot) error
	Stop() error
	Status() core.RecordingSession
}
```

**Step 5: 创建 ports/logging.go**

```go
package ports

// LogPort 日志持久化端口接口
type LogPort interface {
	Start(outputDir string, prefix string) error
	Write(timestamp int64, level string, category string, deviceID string, source string, message string, detail string) error
	Stop() error
	IsActive() bool
	GetOutputDir() string
}
```

---

## Task 4: Adapters 层 — 硬件适配器（核心差异）

**Files:**
- Create: `projects/daq-p1604/apps/desktop-wails/adapters/hardware/p1604_adapter.go`
- Create: `projects/daq-p1604/apps/desktop-wails/adapters/hardware/p1604_scanner.go`
- Create: `projects/daq-p1604/apps/desktop-wails/adapters/hardware/simulated_adapter.go`
- Create: `projects/daq-p1604/apps/desktop-wails/adapters/hardware/simulated_scanner.go`

**Step 1: 创建 p1604_adapter.go**

这是与 T1603 最大的差异。P1604Adapter 需要使用 `shared/device-sdk/go/protocol` 中的帧解析器，处理二进制协议。

关键实现要点：
1. 使用 `sharedproto.FrameReader` 读取 2 字节大端长度前缀帧
2. 使用 `sharedproto.ParseStreamFrame` 解析二进制数据帧（18 通道，前 16 逆序已由 ParseStreamFrame 处理）
3. 使用 `sharedproto.IsASCIIFrame` 区分 ASCII 响应和二进制帧
4. 连接后必须先发 `w1601` 启用长度前缀
5. 采集流程：`c 00 1 FFFF 1 <period> 7 0` → `c 05 1 0810` → `c 01 1`
6. 停止采集：`c 02 1`
7. 18 通道数据：CH1-CH16 压力 + CH17 大气压力 + CH18 大气温度

```go
package hardware

import (
	"fmt"
	"log/slog"
	"net"
	"strings"
	"sync"
	"time"

	sharedproto "shared.local/device-sdk/go/protocol"

	"daq-p1604/core"
	"daq-p1604/ports"
)

const (
	p1604DefaultHost    = "192.168.3.101"
	p1604DefaultPort    = 9000
	p1604ConnectTimeout = 5 * time.Second
	p1604CommandTimeout = 2 * time.Second
	p1604ReadTimeout    = 200 * time.Millisecond
	p1604NumChannels    = 18
	p1604NumPressure    = 16
)

// DeviceLogEntry 设备日志条目
type DeviceLogEntry struct {
	Level    string
	Category string
	DeviceID string
	Message  string
	Detail   string
}

// P1604Adapter DAQ-P-1604 硬件适配器
type P1604Adapter struct {
	mu       sync.RWMutex
	drivers  map[string]*p1604Driver
	status   map[string]*core.DeviceState
	sinks    map[string]func(core.PressureSnapshot)
	channels map[string]chan core.PressureSnapshot
	stopChs  map[string]chan struct{}
	logSink  func(DeviceLogEntry)
}

type p1604Driver struct {
	profile     core.PressureProfile
	conn        net.Conn
	frameReader *sharedproto.FrameReader
	acquiring   bool
}

func NewP1604Adapter() *P1604Adapter {
	return &P1604Adapter{
		drivers:  make(map[string]*p1604Driver),
		status:   make(map[string]*core.DeviceState),
		sinks:    make(map[string]func(core.PressureSnapshot)),
		channels: make(map[string]chan core.PressureSnapshot),
		stopChs:  make(map[string]chan struct{}),
	}
}

var _ ports.DevicePort = (*P1604Adapter)(nil)

func (a *P1604Adapter) SetLogSink(sink func(DeviceLogEntry)) {
	a.mu.Lock()
	a.logSink = sink
	a.mu.Unlock()
}

func (a *P1604Adapter) emitLog(entry DeviceLogEntry) {
	a.mu.RLock()
	sink := a.logSink
	a.mu.RUnlock()
	if sink != nil {
		sink(entry)
	}
}

func (a *P1604Adapter) Connect(profile core.PressureProfile) error {
	a.mu.Lock()
	defer a.mu.Unlock()

	if _, exists := a.drivers[profile.ID]; exists {
		return fmt.Errorf("device %s already connected", profile.ID)
	}

	host := profile.Address
	if host == "" {
		host = p1604DefaultHost
	}
	port := profile.Port
	if port <= 0 {
		port = p1604DefaultPort
	}

	conn, err := net.DialTimeout("tcp", fmt.Sprintf("%s:%d", host, port), p1604ConnectTimeout)
	if err != nil {
		return fmt.Errorf("connect to %s:%d: %w", host, port, err)
	}

	driver := &p1604Driver{
		profile:     profile,
		conn:        conn,
		frameReader: sharedproto.NewFrameReader(conn),
	}

	// 连接后必须先发 w1601 启用长度前缀模式
	if err := driver.sendCommand("w1601"); err != nil {
		conn.Close()
		return fmt.Errorf("enable length prefix: %w", err)
	}
	time.Sleep(50 * time.Millisecond)

	a.drivers[profile.ID] = driver
	a.status[profile.ID] = &core.DeviceState{
		Profile:     profile,
		Status:      core.StatusConnected,
		StatusText:  core.StatusConnected.String(),
		ConnectedAt: core.TimestampMs(),
	}

	a.emitLog(DeviceLogEntry{
		Level: "info", Category: "hardware", DeviceID: profile.ID,
		Message: "Device connected", Detail: fmt.Sprintf("%s:%d", host, port),
	})
	return nil
}

func (a *P1604Adapter) Disconnect(id string) error {
	a.mu.Lock()
	defer a.mu.Unlock()

	_ = a.stopAcquisitionLocked(id)
	driver, ok := a.drivers[id]
	if !ok {
		return nil
	}
	delete(a.drivers, id)
	delete(a.sinks, id)
	if st, exists := a.status[id]; exists {
		st.SetStatus(core.StatusDisconnected)
	}
	return driver.conn.Close()
}

func (a *P1604Adapter) StartAcquisition(id string) (<-chan core.PressureSnapshot, error) {
	a.mu.Lock()
	driver, ok := a.drivers[id]
	if !ok {
		a.mu.Unlock()
		return nil, fmt.Errorf("device %s not connected", id)
	}
	if _, exists := a.channels[id]; exists {
		a.mu.Unlock()
		return nil, fmt.Errorf("device %s already acquiring", id)
	}

	// 配置数据流参数
	periodMs := driver.profile.P1604Cfg.SamplingRate
	if periodMs < 10 {
		periodMs = 100 // 默认 100ms
	}
	if err := driver.sendCommand(fmt.Sprintf("c 00 1 FFFF 1 %d 7 0", periodMs)); err != nil {
		a.mu.Unlock()
		return nil, fmt.Errorf("set stream params: %w", err)
	}
	time.Sleep(50 * time.Millisecond)

	// 配置流返回内容：0810 = 压力 + 大气压力 + 大气温度
	if err := driver.sendCommand("c 05 1 0810"); err != nil {
		a.mu.Unlock()
		return nil, fmt.Errorf("set stream content: %w", err)
	}
	time.Sleep(50 * time.Millisecond)

	// 启动数据流
	if err := driver.sendCommand("c 01 1"); err != nil {
		a.mu.Unlock()
		return nil, fmt.Errorf("start stream: %w", err)
	}

	ch := make(chan core.PressureSnapshot, 8192)
	done := make(chan struct{})
	a.channels[id] = ch
	a.stopChs[id] = done
	driver.acquiring = true

	directSink := func(snapshot core.PressureSnapshot) {
		select {
		case ch <- snapshot:
		case <-done:
		}
	}
	a.sinks[id] = directSink

	if st, exists := a.status[id]; exists {
		st.SetStatus(core.StatusAcquiring)
		st.AcquiringAt = core.TimestampMs()
	}
	a.mu.Unlock()

	// 启动读取循环
	go a.readLoop(id, driver, done)

	return ch, nil
}

func (a *P1604Adapter) StopAcquisition(id string) error {
	a.mu.Lock()
	defer a.mu.Unlock()
	return a.stopAcquisitionLocked(id)
}

func (a *P1604Adapter) stopAcquisitionLocked(id string) error {
	driver, ok := a.drivers[id]
	if !ok {
		return nil
	}

	if driver.acquiring {
		if err := driver.sendCommand("c 02 1"); err != nil {
			slog.Warn("DAQ-P-1604 stop stream command failed", "device", id, "error", err)
		}
		driver.acquiring = false
	}

	if done, ok := a.stopChs[id]; ok {
		close(done)
		delete(a.stopChs, id)
	}
	delete(a.sinks, id)

	if ch, ok := a.channels[id]; ok {
		close(ch)
		delete(a.channels, id)
	}

	if st, exists := a.status[id]; exists {
		st.SetStatus(core.StatusConnected)
	}
	return nil
}

func (a *P1604Adapter) Status(id string) (core.DeviceState, bool) {
	a.mu.RLock()
	defer a.mu.RUnlock()

	st, ok := a.status[id]
	if !ok {
		return core.DeviceState{}, false
	}
	driver, hasDriver := a.drivers[id]
	if hasDriver && driver.acquiring {
		st.SetStatus(core.StatusAcquiring)
	} else if hasDriver {
		st.SetStatus(core.StatusConnected)
	}
	return *st, true
}

func (a *P1604Adapter) ApplyConfig(id string, cfg core.P1604Config) error {
	a.mu.Lock()
	if st, exists := a.status[id]; exists {
		st.Profile.P1604Cfg = cfg
	}
	a.mu.Unlock()
	// P1604 配置在下次 StartAcquisition 时生效（需要重新配置流参数）
	return nil
}

func (a *P1604Adapter) SetDataSink(id string, sink func(core.PressureSnapshot)) {
	a.mu.Lock()
	defer a.mu.Unlock()
	a.sinks[id] = sink
}

// readLoop 读取设备数据帧
func (a *P1604Adapter) readLoop(id string, driver *p1604Driver, stop <-chan struct{}) {
	for {
		select {
		case <-stop:
			return
		default:
			driver.conn.SetReadDeadline(time.Now().Add(p1604ReadTimeout))
			payload, err := driver.frameReader.ReadFrame()
			if err != nil {
				if netErr, ok := err.(net.Error); ok && netErr.Timeout() {
					continue
				}
				// 连接断开
				a.mu.Lock()
				st, exists := a.status[id]
				if !exists || st.Status == core.StatusDisconnected {
					a.mu.Unlock()
					return
				}
				delete(a.sinks, id)
				if done, ok := a.stopChs[id]; ok {
					close(done)
					delete(a.stopChs, id)
				}
				if ch, ok := a.channels[id]; ok {
					close(ch)
					delete(a.channels, id)
				}
				driver.acquiring = false
				st.SetStatus(core.StatusConnected)
				st.AcquiringAt = 0
				a.mu.Unlock()
				return
			}
			if len(payload) > 0 {
				a.processPayload(id, payload)
			}
		}
	}
}

// processPayload 处理接收到的数据帧
func (a *P1604Adapter) processPayload(id string, data []byte) {
	// 区分 ASCII 响应和二进制帧
	if sharedproto.IsASCIIFrame(data) {
		// ASCII 响应（命令确认等），忽略
		return
	}

	// 解析二进制数据帧
	channels, err := sharedproto.ParseStreamFrame(data)
	if err != nil {
		a.emitLog(DeviceLogEntry{
			Level: "debug", Category: "hardware", DeviceID: id,
			Message: "Frame parse error", Detail: err.Error(),
		})
		return
	}

	if len(channels) != p1604NumChannels {
		a.emitLog(DeviceLogEntry{
			Level: "warn", Category: "hardware", DeviceID: id,
			Message: "Unexpected channel count", Detail: fmt.Sprintf("expected %d, got %d", p1604NumChannels, len(channels)),
		})
		return
	}

	a.mu.RLock()
	sink := a.sinks[id]
	unit := "psi"
	if st, ok := a.status[id]; ok && st.Profile.P1604Cfg.Unit != "" {
		unit = st.Profile.P1604Cfg.Unit
	}
	a.mu.RUnlock()

	if sink != nil {
		sink(core.PressureSnapshot{
			DeviceID:  id,
			Timestamp: core.TimestampMs(),
			Values:    channels,
			Unit:      unit,
		})
	}
}

// sendCommand 发送命令到设备
func (d *p1604Driver) sendCommand(cmd string) error {
	if d.conn == nil {
		return fmt.Errorf("not connected")
	}
	d.conn.SetWriteDeadline(time.Now().Add(p1604CommandTimeout))
	_, err := d.conn.Write([]byte(cmd + "\r\n"))
	return err
}

// isConnectionFault 判断是否为连接故障
func isConnectionFault(err error) bool {
	if err == nil {
		return false
	}
	message := strings.ToLower(err.Error())
	return strings.Contains(message, "i/o timeout") ||
		strings.Contains(message, "broken pipe") ||
		strings.Contains(message, "reset by peer") ||
		strings.Contains(message, "device disconnected")
}
```

**Step 2: 创建 p1604_scanner.go**

P1604 设备发现使用 UDP 广播 `psi9000` 到端口 7000，在端口 7001 监听响应。

```go
package hardware

import (
	"fmt"
	"net"
	"strconv"
	"strings"
	"sync"
	"time"

	"daq-p1604/core"
)

const (
	p1604DiscoveryCmd     = "psi9000"
	p1604DiscoverySendPort = 7000
	p1604DiscoveryRecvPort = 7001
	p1604ScanTimeout      = 3 * time.Second
	p1604LimitedBroadcast = "255.255.255.255"
	p1604ScanResultPrefix = "scan-daq-p-1604"
)

type P1604Scanner struct {
	timeout time.Duration
}

func NewP1604Scanner() *P1604Scanner {
	return &P1604Scanner{
		timeout: p1604ScanTimeout,
	}
}

func (s *P1604Scanner) Scan() ([]core.ScanResult, error) {
	// 在 7001 端口监听响应
	conn, err := net.ListenPacket("udp4", fmt.Sprintf(":%d", p1604DiscoveryRecvPort))
	if err != nil {
		return nil, fmt.Errorf("udp listen on %d: %w", p1604DiscoveryRecvPort, err)
	}
	defer conn.Close()

	if err := conn.SetDeadline(time.Now().Add(s.timeout)); err != nil {
		return nil, fmt.Errorf("set deadline: %w", err)
	}

	// 向所有网段广播地址发送发现命令
	cmd := []byte(p1604DiscoveryCmd)
	targets := broadcastTargets()
	for _, t := range targets {
		addr := &net.UDPAddr{IP: net.ParseIP(t), Port: p1604DiscoverySendPort}
		if addr.IP == nil {
			continue
		}
		conn.WriteTo(cmd, addr)
	}

	var mu sync.Mutex
	var results []core.ScanResult
	seen := make(map[string]bool)
	buf := make([]byte, 1024)

	for {
		n, remote, err := conn.ReadFrom(buf)
		if err != nil {
			if netErr, ok := err.(net.Error); ok && netErr.Timeout() {
				break
			}
			break
		}

		result := parseP1604Response(buf[:n], remote.String())
		if result == nil {
			continue
		}

		mu.Lock()
		if !seen[result.ID] {
			seen[result.ID] = true
			results = append(results, *result)
		}
		mu.Unlock()
	}

	if results == nil {
		results = []core.ScanResult{}
	}
	return results, nil
}

// parseP1604Response 解析 P1604 设备发现响应
// 响应 CSV 格式：<IP>,<MAC>,,<序列号>,<固件版本>,,<端口>,<子网掩码>,<网关>
func parseP1604Response(data []byte, remoteAddr string) *core.ScanResult {
	msg := strings.TrimSpace(string(data))
	parts := strings.Split(msg, ",")
	if len(parts) < 9 {
		return nil
	}

	ip := strings.TrimSpace(parts[0])
	if ip == "" {
		host, _, _ := net.SplitHostPort(remoteAddr)
		ip = host
	}

	port := p1604DefaultPort
	if p, err := strconv.Atoi(strings.TrimSpace(parts[7])); err == nil && p > 0 {
		port = p
	}

	mac := strings.TrimSpace(parts[1])
	serial := strings.TrimSpace(parts[3])
	firmware := strings.TrimSpace(parts[4])

	result := &core.ScanResult{
		Name:            "Discovered DAQ-P-1604",
		Address:         ip,
		Port:            port,
		MacAddress:      mac,
		SerialNumber:    serial,
		FirmwareVersion: firmware,
	}

	if mac != "" {
		result.ID = fmt.Sprintf("%s-%s", p1604ScanResultPrefix, mac)
	} else {
		result.ID = fmt.Sprintf("%s-%s-%d", p1604ScanResultPrefix, ip, port)
	}
	return result
}
```

**Step 3: 创建 simulated_adapter.go**

模拟适配器，生成 18 通道压力数据（16 压力 + 大气压力 + 大气温度）。

```go
package hardware

import (
	"fmt"
	"math"
	"sync"
	"time"

	"daq-p1604/core"
	"daq-p1604/ports"
)

type SimulatedAdapter struct {
	mu       sync.RWMutex
	status   map[string]*core.DeviceState
	sinks    map[string]func(core.PressureSnapshot)
	channels map[string]chan core.PressureSnapshot
	stopChs  map[string]chan struct{}
}

func NewSimulatedAdapter() *SimulatedAdapter {
	return &SimulatedAdapter{
		status:   make(map[string]*core.DeviceState),
		sinks:    make(map[string]func(core.PressureSnapshot)),
		channels: make(map[string]chan core.PressureSnapshot),
		stopChs:  make(map[string]chan struct{}),
	}
}

var _ ports.DevicePort = (*SimulatedAdapter)(nil)

func (a *SimulatedAdapter) Connect(profile core.PressureProfile) error {
	a.mu.Lock()
	defer a.mu.Unlock()
	if _, exists := a.status[profile.ID]; exists {
		return fmt.Errorf("device %s already connected", profile.ID)
	}
	a.status[profile.ID] = &core.DeviceState{
		Profile:     profile,
		Status:      core.StatusConnected,
		StatusText:  core.StatusConnected.String(),
		ConnectedAt: core.TimestampMs(),
	}
	return nil
}

func (a *SimulatedAdapter) Disconnect(id string) error {
	a.mu.Lock()
	defer a.mu.Unlock()
	a.stopAcquisitionLocked(id)
	if st, exists := a.status[id]; exists {
		st.SetStatus(core.StatusDisconnected)
	}
	return nil
}

func (a *SimulatedAdapter) StartAcquisition(id string) (<-chan core.PressureSnapshot, error) {
	a.mu.Lock()
	defer a.mu.Unlock()
	if _, exists := a.status[id]; !exists {
		return nil, fmt.Errorf("device %s not connected", id)
	}
	if _, exists := a.channels[id]; exists {
		return nil, fmt.Errorf("device %s already acquiring", id)
	}
	ch := make(chan core.PressureSnapshot, 64)
	done := make(chan struct{})
	a.channels[id] = ch
	a.stopChs[id] = done
	if st, exists := a.status[id]; exists {
		st.SetStatus(core.StatusAcquiring)
		st.AcquiringAt = core.TimestampMs()
	}
	t0 := time.Now()
	go a.simulateLoop(id, ch, done, t0)
	return ch, nil
}

func (a *SimulatedAdapter) simulateLoop(id string, ch chan<- core.PressureSnapshot, done <-chan struct{}, t0 time.Time) {
	ticker := time.NewTicker(200 * time.Millisecond)
	defer ticker.Stop()
	for {
		select {
		case <-done:
			return
		case t := <-ticker.C:
			elapsed := t.Sub(t0).Seconds()
			values := make([]float64, 18)
			// CH1-CH16: 压力值（模拟 0-100 psi 范围波动）
			for i := 0; i < 16; i++ {
				base := 50.0 + float64(i)*2.5
				values[i] = base + 10*math.Sin(elapsed*0.5+float64(i)*0.3) + 2*math.Sin(elapsed*3+float64(i)*0.7)
			}
			// CH17: 大气压力（约 101325 Pa）
			values[16] = 101325.0 + 50*math.Sin(elapsed*0.2)
			// CH18: 大气温度（约 25°C）
			values[17] = 25.0 + 2*math.Sin(elapsed*0.3)
			ch <- core.PressureSnapshot{
				DeviceID:  id,
				Timestamp: t.UnixMilli(),
				Values:    values,
				Unit:      "psi",
			}
		}
	}
}

func (a *SimulatedAdapter) stopAcquisitionLocked(id string) {
	if done, ok := a.stopChs[id]; ok {
		close(done)
		delete(a.stopChs, id)
	}
	delete(a.sinks, id)
	if ch, ok := a.channels[id]; ok {
		close(ch)
		delete(a.channels, id)
	}
	if st, exists := a.status[id]; exists {
		st.SetStatus(core.StatusConnected)
	}
}

func (a *SimulatedAdapter) StopAcquisition(id string) error {
	a.mu.Lock()
	defer a.mu.Unlock()
	a.stopAcquisitionLocked(id)
	return nil
}

func (a *SimulatedAdapter) Status(id string) (core.DeviceState, bool) {
	a.mu.RLock()
	defer a.mu.RUnlock()
	st, ok := a.status[id]
	if !ok {
		return core.DeviceState{}, false
	}
	st.StatusText = st.Status.String()
	return *st, true
}

func (a *SimulatedAdapter) ApplyConfig(id string, cfg core.P1604Config) error {
	a.mu.Lock()
	defer a.mu.Unlock()
	if st, exists := a.status[id]; exists {
		if cfg.SamplingRate < 10 {
			cfg.SamplingRate = 100
		}
		if cfg.Unit == "" {
			cfg.Unit = "psi"
		}
		st.Profile.P1604Cfg = cfg
		return nil
	}
	return fmt.Errorf("device %s not connected", id)
}

func (a *SimulatedAdapter) SetDataSink(id string, sink func(core.PressureSnapshot)) {
	a.mu.Lock()
	defer a.mu.Unlock()
	a.sinks[id] = sink
}
```

**Step 4: 创建 simulated_scanner.go**

```go
package hardware

import "daq-p1604/core"

type SimulatedScanner struct{}

func NewSimulatedScanner() *SimulatedScanner {
	return &SimulatedScanner{}
}

func (s *SimulatedScanner) Scan() ([]core.ScanResult, error) {
	return []core.ScanResult{
		{
			ID:              "scan-daq-p-1604-192.168.3.101-9000",
			Name:            "Discovered DAQ-P-1604",
			Address:         "192.168.3.101",
			Port:            9000,
			MacAddress:      "aa:bb:cc:dd:ee:01",
			SerialNumber:    "SIM-P001",
			FirmwareVersion: "v1.0-sim",
		},
	}, nil
}
```

---

## Task 5: Adapters 层 — 配置、录制、日志适配器

**Files:**
- Create: `projects/daq-p1604/apps/desktop-wails/adapters/config/json_config.go`
- Create: `projects/daq-p1604/apps/desktop-wails/adapters/recording/csv_recorder.go`
- Create: `projects/daq-p1604/apps/desktop-wails/adapters/logging/log_file_writer.go`

**Step 1: 创建 config/json_config.go**

与 T1603 的 `json_config.go` 完全一致，仅将 `core.TemperatureProfile` 替换为 `core.PressureProfile`。

**Step 2: 创建 recording/csv_recorder.go**

与 T1603 的 `csv_recorder.go` 基本一致，但 CSV 表头改为 18 通道：
- `Timestamp, CH01, CH02, ..., CH16, CH17_AtmPressure, CH18_AtmTemp`
- `Write` 方法接收 `core.PressureSnapshot`

**Step 3: 创建 logging/log_file_writer.go**

与 T1603 的 `log_file_writer.go` 完全一致（日志逻辑与设备类型无关）。

---

## Task 6: Usecase 层

**Files:**
- Create: `projects/daq-p1604/apps/desktop-wails/usecase/device_usecase.go`
- Create: `projects/daq-p1604/apps/desktop-wails/usecase/recording_usecase.go`
- Create: `projects/daq-p1604/apps/desktop-wails/usecase/log_usecase.go`

**Step 1: 创建 device_usecase.go**

与 T1603 的 `device_usecase.go` 结构一致，类型替换：
- `core.TemperatureProfile` → `core.PressureProfile`
- `core.TemperatureSnapshot` → `core.PressureSnapshot`
- `core.T1603Config` → `core.P1604Config`

**Step 2: 创建 recording_usecase.go**

与 T1603 完全一致，类型替换 `TemperatureSnapshot` → `PressureSnapshot`。

**Step 3: 创建 log_usecase.go**

与 T1603 完全一致（日志逻辑与设备类型无关）。

---

## Task 7: Backend 层

**Files:**
- Create: `projects/daq-p1604/apps/desktop-wails/backend/app.go`

**Step 1: 创建 backend/app.go**

与 T1603 的 `backend/app.go` 结构一致，类型替换：
- `core.TemperatureSnapshot` → `core.PressureSnapshot`
- `core.T1603Config` → `core.P1604Config`
- 事件名保持 `daq:payload`、`daq:log`、`daq:recording-status`
- `ApplyConfig` 方法签名改为接收 `core.P1604Config`
- 日志消息中 "DAQ-T-1603" → "DAQ-P-1604"

---

## Task 8: Frontend — 基础配置和类型

**Files:**
- Create: `projects/daq-p1604/apps/desktop-wails/frontend/package.json`
- Create: `projects/daq-p1604/apps/desktop-wails/frontend/tsconfig.json`
- Create: `projects/daq-p1604/apps/desktop-wails/frontend/vite.config.ts`
- Create: `projects/daq-p1604/apps/desktop-wails/frontend/index.html`
- Create: `projects/daq-p1604/apps/desktop-wails/frontend/src/main.ts`
- Create: `projects/daq-p1604/apps/desktop-wails/frontend/src/env.d.ts`
- Create: `projects/daq-p1604/apps/desktop-wails/frontend/src/styles.css`

**Step 1: 创建 package.json**

与 T1603 的 `package.json` 一致，名称改为 `daq-p1604-frontend`。

**Step 2-7: 创建其余基础配置文件**

与 T1603 对应文件一致，仅替换项目名称。

---

## Task 9: Frontend — Bridge 和类型定义

**Files:**
- Create: `projects/daq-p1604/apps/desktop-wails/frontend/src/bridge/deviceBridge.ts`
- Create: `projects/daq-p1604/apps/desktop-wails/frontend/src/bridge/logBridge.ts`
- Create: `projects/daq-p1604/apps/desktop-wails/frontend/src/bridge/recordingBridge.ts`

**Step 1: 创建 deviceBridge.ts**

与 T1603 的 `deviceBridge.ts` 结构一致，类型替换：
- `T1603Config` → `P1604Config`（字段：samplingRate, unit, autoConnect）
- `TemperatureProfile` → `PressureProfile`（p1604Config 替代 t1603Config）
- `TemperatureSnapshot` → `PressureSnapshot`
- `ChannelConfig`：移除 `thermocoupleType` 字段
- `ApplyConfig` 方法签名改为接收 `P1604Config`

```typescript
export interface P1604Config {
  samplingRate: number   // 采样周期（毫秒）
  unit: string           // 压力单位
  autoConnect: boolean   // 启动时自动连接
}

export interface PressureProfile {
  id: string
  name: string
  address: string
  port: number
  samplingRate: number
  channels: ChannelConfig[]
  p1604Config: P1604Config
  createdAt: number
}

export interface PressureSnapshot {
  deviceId: string
  timestamp: number
  hardwareTimestamp: number
  values: number[]  // 18 通道
  unit: string
}
```

**Step 2-3: 创建 logBridge.ts 和 recordingBridge.ts**

与 T1603 完全一致。

---

## Task 10: Frontend — Stores

**Files:**
- Create: `projects/daq-p1604/apps/desktop-wails/frontend/src/stores/deviceStore.ts`
- Create: `projects/daq-p1604/apps/desktop-wails/frontend/src/stores/displayStore.ts`
- Create: `projects/daq-p1604/apps/desktop-wails/frontend/src/stores/logStore.ts`
- Create: `projects/daq-p1604/apps/desktop-wails/frontend/src/stores/recordingStore.ts`

**Step 1: 创建 deviceStore.ts**

与 T1603 的 `deviceStore.ts` 结构一致，关键差异：
- 默认通道数 18（16 压力 + 大气压力 + 大气温度）
- 默认单位 `psi` 而非 `°C`
- 默认精度：压力 3 位、大气压 0 位、温度 2 位
- `T1603Config` → `P1604Config`
- `TemperatureProfile` → `PressureProfile`
- `TemperatureSnapshot` → `PressureSnapshot`
- `defaultChannels()` 生成 18 个通道
- `defaultP1604Config()` 替代 `defaultT1603Config()`
- `addProfile` 中 ID 前缀改为 `p1604_`
- `applyConfig` 使用 `p1604Defaults()`

```typescript
function defaultChannels() {
  return Array.from({ length: 18 }, (_, i) => ({
    index: i,
    name: i < 16 ? `通道 ${i + 1}` : (i === 16 ? '大气压力' : '大气温度'),
    enabled: true,
    unit: i < 16 ? 'psi' : (i === 16 ? 'Pa' : '°C'),
    color: CHANNEL_COLORS[i % CHANNEL_COLORS.length],
    precision: i < 16 ? 3 : (i === 16 ? 0 : 2),
    rangeMin: 0,
    rangeMax: i < 16 ? 200 : (i === 16 ? 200000 : 100),
  }))
}

function defaultP1604Config(): P1604Config {
  return {
    samplingRate: 100,  // 100ms 周期
    unit: 'psi',
    autoConnect: false,
  }
}
```

**Step 2-4: 创建 displayStore.ts、logStore.ts、recordingStore.ts**

与 T1603 完全一致。

---

## Task 11: Frontend — 组件

**Files:**
- Create: `projects/daq-p1604/apps/desktop-wails/frontend/src/components/device/ChannelCard.vue`
- Create: `projects/daq-p1604/apps/desktop-wails/frontend/src/components/device/ChannelGrid.vue`
- Create: `projects/daq-p1604/apps/desktop-wails/frontend/src/components/device/CustomSelect.vue`
- Create: `projects/daq-p1604/apps/desktop-wails/frontend/src/components/device/DaqP1604Config.vue`
- Create: `projects/daq-p1604/apps/desktop-wails/frontend/src/components/device/RealtimeChart.vue`
- Create: `projects/daq-p1604/apps/desktop-wails/frontend/src/components/device/ScanResultList.vue`
- Create: `projects/daq-p1604/apps/desktop-wails/frontend/src/components/layout/AppShell.vue`
- Create: `projects/daq-p1604/apps/desktop-wails/frontend/src/components/layout/DeviceSidebar.vue`
- Create: `projects/daq-p1604/apps/desktop-wails/frontend/src/components/layout/LogPanel.vue`
- Create: `projects/daq-p1604/apps/desktop-wails/frontend/src/components/layout/MainBottomBar.vue`
- Create: `projects/daq-p1604/apps/desktop-wails/frontend/src/components/layout/MainTopBar.vue`

**Step 1: 创建 DaqP1604Config.vue**

与 T1603 的 `DaqT1603Config.vue` 结构一致，关键差异：
- 移除热电偶类型选择（P1604 无此概念）
- 增加压力单位选择（psi/Pa/kPa/MPa/kgf/cm²）
- 采样率改为采样周期（毫秒），范围 10-60000
- 通道列表改为 18 通道
- 通道配置中移除热电偶类型，保留名称、启用/禁用、精度
- CH17 和 CH18 标注为"大气压力"和"大气温度"

**Step 2: 创建 ChannelCard.vue**

与 T1603 一致，无需修改。

**Step 3: 创建 ChannelGrid.vue**

与 T1603 一致，但适配 18 通道布局。

**Step 4: 创建其余组件**

与 T1603 对应组件一致，替换 T1603 → P1604 相关文案。

---

## Task 12: Frontend — Views、Composables、App

**Files:**
- Create: `projects/daq-p1604/apps/desktop-wails/frontend/src/views/MonitorView.vue`
- Create: `projects/daq-p1604/apps/desktop-wails/frontend/src/composables/useTheme.ts`
- Create: `projects/daq-p1604/apps/desktop-wails/frontend/src/App.vue`

**Step 1: 创建 MonitorView.vue**

与 T1603 的 `MonitorView.vue` 结构一致，替换类型引用。

**Step 2: 创建 useTheme.ts**

与 T1603 完全一致。

**Step 3: 创建 App.vue**

与 T1603 的 `App.vue` 结构一致，替换类型引用：
- `TemperatureSnapshot` → `PressureSnapshot`
- `DeviceLogEvent` 保持一致

---

## Task 13: 项目元数据文件

**Files:**
- Create: `projects/daq-p1604/AGENTS.md`
- Create: `projects/daq-p1604/CHANGELOG.md`
- Create: `projects/daq-p1604/VERSION`

**Step 1: 创建 AGENTS.md**

基于 T1603 的 AGENTS.md，替换项目名称和设备描述。

**Step 2: 创建 CHANGELOG.md**

```markdown
# Changelog

## 0.1.0

- 初始版本：DAQ-P-1604 压力采集桌面应用
- 18 通道数据采集（16 压力 + 大气压力 + 大气温度）
- TCP 二进制协议通信
- UDP 设备发现
- CSV 数据录制
- 实时波形显示
- 设备配置管理
```

**Step 3: 创建 VERSION**

```
0.1.0
```

---

## Task 14: go.work 更新和构建验证

**Step 1: 更新 go.work**

在 workspace 根目录的 `go.work` 中添加新项目：

```
use (
    // ... 现有项目
    ./projects/daq-p1604/apps/desktop-wails
)
```

**Step 2: 运行 Go 模块整理**

```powershell
cd projects\daq-p1604\apps\desktop-wails
go mod tidy
```

**Step 3: 运行 Go 编译验证**

```powershell
cd projects\daq-p1604\apps\desktop-wails
go build -buildvcs=false ./...
go vet ./...
```

**Step 4: 运行结构验证**

```powershell
powershell -File .\scripts\validate-structure.ps1
```

**Step 5: 安装前端依赖并构建**

```powershell
cd projects\daq-p1604\apps\desktop-wails\frontend
npm install --no-audit --no-fund
npm run typecheck
npm run build
```

**Step 6: Wails 生成绑定**

```powershell
cd projects\daq-p1604\apps\desktop-wails
wails generate module
```

---

## Task 15: 集成测试

**Step 1: 运行 Go 测试**

```powershell
cd projects\daq-p1604\apps\desktop-wails
go test ./...
```

**Step 2: 运行前端类型检查**

```powershell
cd projects\daq-p1604\apps\desktop-wails\frontend
npm run typecheck
```

**Step 3: 模拟模式启动验证**

```powershell
cd projects\daq-p1604\apps\desktop-wails
$env:DAQ_P1604_MODE="simulated"
wails dev
```

验证：
- 应用正常启动
- 模拟设备出现在设备列表
- 采集启动/停止正常
- 18 通道数据正常显示
- CSV 录制正常
