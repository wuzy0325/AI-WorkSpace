# Code Review: 9 Unpushed Commits (2026-06-22)

**Branch:** `master` (ahead of `origin/master` by 9 commits)
**Scope:** 170 files, +12,182 / -4,101 lines
**Review Date:** 2026-06-22（第二轮：含修复后验证）

---

## Context

覆盖三个项目 (`daq-t1603`, `daq-p1604`, `wind-daq`)、shared SDK、workspace 基础设施。核心改动：

- T1603 硬件协议可靠性修复（`\n` 后缀移除、强制二进制模式、frame 自动重同步）
- Wind-DAQ traversal 模块拆分（886 行单文件 → 6 个聚焦文件）
- 新增 core 模块：实时插值缓存、资源互斥锁、扫描阀适配器
- 五孔探针插值算法性能优化（解析反演 O(1)、预计算 Ka 范围、预建扩展网格）
- Wind-DAQ 运动控制器独立窗口（多进程 + parent watchdog）
- 前端 composables 提取与 UI 优化
- 导入方向校验脚本 + golangci-lint 配置

### Commit Summary

| Commit | 范围 |
|--------|------|
| `b7b6706` | docs(daq-t1603): drop simulated-mode references after adapter removal |
| `f46f4ca` | fix(wind-daq): traversal/throttler/watchdog correctness fixes |
| `62f2947` | fix(daq-t1603): csv recording data loss + adapter deadlock |
| `76d0edb` | refactor: wind-daq traversal split + 多项目 UI/adapter 改动 |
| `4666f2e` | fix(t1603): protocol/hardware reliability fixes from code review |
| `b8884c0` | 设置命令后概率采集数据解析乱码 |
| `72530c3` | fix(wind-daq): add SetCsvWriter, add running guard, remove dead code |
| `36c2f5c` | fix(daq-t1603): remove \n from hardware commands, force binary format |
| `1487d3b` | fix(daq-t1603): correct status flicker and frequency display |
| `ddb06ff` | chore: workspace.structure.json supplement |

---

## 1. Correctness

### 亮点

- **`\n` 从硬件命令中移除 [关键修复]**: `SendCommand`, `SendCommandIdle`, `SendCommandExact`, `writeCommandOnly` 全部从 `cmd + "\n"` 改为裸 `cmd`。这是 frame 解析偶发乱码的根因——末尾 `\n` 可能被当作二进制帧头的一部分或被设备回射额外字节。diff 新加了 200ms ACK 延迟注释（实测 80-180ms）。

- **强制二进制格式 (BIN=1, TIME=0, HEAD=0) [关键修复]**: `syncHardwareConfig()` 中三条 `@fe` 命令在配置同步前强制设备进入纯 64 字节二进制模式。三条全部成功才生效；部分失败时 `cfg.BinaryFormat` 不变，FrameReader 不会误解析。

- **72 字节时间戳二进制帧支持**: `ParseTCPFrameEx` 处理 `TCPFrameSizeWithTimestamp`（72 字节：8 字节时间戳头 + 64 字节 float32）。`readFrameFixed()` 正确路由。72 字节固定帧路径规避了变长换行分隔解析的脆弱性。

- **零字节 TCP 读取防御**: `readFrameFixed()` 和 `readFrameVariable()` 处理 `conn.Read()` 返回 `(0, nil)` 时 `continue` 而不是返回错误。防止瞬态零字节 TCP 段终止采集循环。

- **连续帧错误自动重同步**: `processPayload()` 跟踪 `consecutiveFrameErrors`，连续 5 次失败后触发 `frameReader.Resync()`，跳过 1 字节重新对齐帧边界。

- **资源锁防止并发工作流**: `resourcelock.Service` 通过带 TTL 的命名锁在 traversal/calibration 间互斥。`Start()` 获取 `"workflow:traversal"` 锁；`Stop()`/`finalizeSink()` 释放。崩溃后 TTL 自动过期。

- **Sink Finalize 覆盖所有退出路径**: traversal 在 `Stop()`（锁外）和 `defer m.finalizeSink()` in `RunTraversalLoop` 中调用 `sink.FinalizeTraversal()`。无论遍历如何结束，CSV 缓冲区都会刷盘。

- **数据验证支持重试**: `RunCurrentPoint` 支持 `validation.onInvalid == "retry"` 及可配置重试次数。重试前等待 `retryWaitInterval`(200ms) 让设备产出新数据帧。

- **[已修复] `ResumeFromCheckpoint` 资源锁获取**: `traversal_checkpoint.go:160` 在设置 `StateRunning` 前调用了 `resourcelock.Default().Acquire(traversalLockResource, cp.TaskID, 24*time.Hour)`。与 `Start()` 行为一致。**编译验证通过，测试通过。**

### 需关注的问题

- **Medium: `isStable()` 在近零值时百分比变化可能失稳。** `prevVal == 0` 时切换到绝对阈值检查，但 `prevVal` 极小时（如 1e-6），`math.Abs((curVal-prevVal)/prevVal) * 100` 可能溢出。建议对分母做最小值截断或一致使用绝对阈值。

- **Medium: `InterpolationCache.evictLRULocked()` 每次淘汰都对所有 key 排序。** 256 条目时每次 insert 触发 O(256 log 256) ≈ 2,048 次比较，在热路径中。考虑用最小堆或双向链表维护访问顺序。

- **Low: `Throttler` timerGen 方案正确但有复杂上下文。** `timerGen` 机制防止陈旧回调，但 `t.pendingTimer.Stop()`（在 `prepareFlushLocked` 中）和 timer 的 `AfterFunc` 回调获取 `t.mu` 之间存在窗口。`gen != t.timerGen` 检查已正确处理。

### 预存数据竞态（非本次引入）

`shared/device-sdk/go/daq/hardware/daq_t1603.go` 的 `readLoop`/`processPayload` 中，`d.conn`、`d.frameReader` 的读写存在预存数据竞态，src 在本次 diff 之前就存在：

| 位置 | 问题 | 严重度 |
|------|------|--------|
| `readLoop:461` | `conn := d.conn` 未持锁读；`Connect`/`Disconnect` 在锁下写 | HIGH |
| `readLoop:510` | `d.frameReader.ReadFrame()` 未持锁；`stopAcquisitionLocked`/`Disconnect` 在锁下写/变 | HIGH |
| `processPayload:567` | `d.frameReader != nil` 未持锁读 | HIGH |
| `processPayload:571` | `d.frameReader.Resync()` 未持锁调用 | HIGH |

这些是预存问题，不属于本 PR 引入的回退。建议单独提 issue 修复。

---

## 2. 可读性与简洁性

### 亮点

- **traversal.go 拆分优秀**：6 个文件。每个文件有包级 doc comment 说明职责。教科书级重构。

- **时间常量类型化**：`motionCompleteTimeoutMs = 120000` → `motionCompleteTimeout = 120 * time.Second`。使用 `time.Duration` 常量消除裸整数被误当纳秒的风险。

- **`roleToLabel()` 显式映射**：替代了依赖通道索引顺序映射 P1..P5 的脆弱假设。通道通过 role 字符串显式映射。

- **`solveBilinearInverse()` 文档完善**：引用了 Heckbert 1989 文献，清晰推导了双线性逆函数的二次方程。

- **`_ =` 错误抑制有注释**：多数位置有注释说明为什么安全忽略（best-effort cleanup、幂等性等）。

### Nit（轻微建议）

- **Nit: `traversal_config.go` 中 `ParseAndStartTraversal` 嵌套过深。** `LayoutConfig` 构造用了 50+ 行匿名函数字面量。可考虑提取 `configFromAPI()` 辅助函数。

- **Nit: `scanvalve/scanvalve.go` 中文错误消息。** `ValidatePressures()` 返回 `"P1 超出范围"`、`"非有限数"` 等中文消息，与 Go 代码库其余部分不一致。建议改为英文或使用结构化错误码。

- **Nit: `parent_watchdog_windows.go` 使用 `syscall` 包。** `syscall.ERROR_ACCESS_DENIED` 在 `golang.org/x/sys/windows` 中有等价常量。`syscall` 在 Go 文档中标记为 deprecated。

---

## 3. 架构

### 亮点

- **ResourceLock 在 core/**: 作为纯进程内协调原语，无 I/O。`sync.Once` 全局单例模式正确。

- **Realtime cache 在 core/**: `InterpolationCache` 和 `Throttler` 是纯计算/数据结构，与用例逻辑解耦。

- **ScanValve 在 core/**: 扫描阀配置、推荐公式、压力校验是领域逻辑，通过 `traversal.DataValidationConfig` 与上层桥接。

- **新 ports 接口**: `ports.CheckpointStore`, `ports.AppConfigStore` 正确定义在 ports 层。构造函数注入。适配器 (`file_checkpoint_store.go`) 在 `adapters/storage/`。

- **运动控制器多进程独立窗口**: `--motion-only` + `ModeMotion` 干净地分离运动控制器窗口与主 DAQ 窗口。parent watchdog 防止父进程被强杀后留下孤儿。

- **`validate-import-direction.ps1` 补充 golangci-lint**: golangci-lint 的 depguard 检查 stdlib 包；此脚本处理跨项目适配器路径通配。二者共同执行 CLAUDE.md 的六边形架构约束。

---

## 4. 安全

### 亮点

- 无 secrets/credentials/API keys 泄露。
- 无 SQL 注入向量（未引入数据库）。
- `resourcelock` 防止并发硬件访问，对电机设备是安全要求。

### 关注

- **Low: `parent_watchdog.go` 直接调用 `os.Exit(0)`。** 这会跳过所有 `defer` 语句。500ms 优雅关停期缓解了部分问题，但不能保证所有 pending 资源清理完成。

---

## 5. 性能

### 亮点

- **双线性逆解析 (O(1)) 替代子网格暴力搜索 (O(N))**: `solveBilinearInverse()` 将双线性映射反演解析为二次方程求解。
- **预计算 Ka 范围**: `computeKaRange()` 在网格构建时一次性计算。`findGridCell()` 利用此将搜索缩小至约 2 条邻域 alpha 带。
- **预建扩展网格 cell**: `preBuildExtendedCells()` 在加载阶段构建，避免每次 `Calculate` 重复执行 `estimateGridPoint`。
- **手写 4 元素浮点数降序排序**: 5 次比较的固定排序网络，消除 slice 堆分配和接口分发开销。
- **`interpolationBounds()` 二分搜索**: O(log n) 替代 O(n) 线性扫描。
- **`Throttler` 防止 UI 过载**: 限制实时插值更新频率并计数跳帧。

---

## 6. 前端代码质量

### TypeScript/Vue 审查结果

| 严重度 | 文件 | 行号 | 问题 |
|--------|------|------|------|
| **Medium** | `api/motionApi.ts` | 105, 194, 216, 236 | 4 处静默空 catch 块吞掉后端 profile 操作失败 |
| **Medium** | `api/motionApi.ts` | 406, 408, 434, 441 | 4 处静默空 catch 块吞掉监听回调/轮询异常 |
| **Low** | `stores/traversalStore.ts` | 33, 34 | 未使用的 import `DataValidationConfig`, `StabilizationConfig` |
| **Low** | `composables/useTraversalSimulation.ts` | 25 | 未使用的 import `TraversalCompleteEvent` |
| **Low** | `composables/useTraversalSimulation.ts` | 142 | 未使用的 `reject` 参数（Promise 从不 reject） |
| **Low** | `composables/useFileImport.ts` | 107 | `(f as any).path` 应使用 proper 扩展接口 `FileWithPath` |
| **Low** | `stores/i18nStore.ts` | 809 | 静默吞掉 localStorage 写入失败 |
| **Low** | `api/traversalApi.ts` | 82 | `console.error` 在 production 代码中（可接受但非结构化） |

无 `console.log`、无直接 DOM 操作滥用（全部为标准文件选择器/focus trap/screenshot 导出模式）。`setTimeout`/`setInterval` 均有适当清理。

---

## 7. 缺失 `_ =` 注释

审查发现 5 处 `_ =` 错误抑制注释不够充分：

| 文件 | 行 | 代码 | 建议 |
|------|-----|------|------|
| `traversal.go` | 99 | `_ = m.configStore.SaveConfig(...)` | 注明 "best-effort persistence, non-critical" |
| `traversal.go` | 234 | `_ = resourcelock.Default().Release(...)` | 已有注释但建议写清 "best-effort cleanup, lock will expire via TTL" |
| `traversal.go` | 319 | `_ = resourcelock.Default().Release(...)` | 同上 |
| `traversal_view.go` | 27 | `_ = resourcelock.Default().Release(...)` | 同上 |
| `traversal_checkpoint.go` | 120 | `_ = store.Remove(path)` | **完全无注释**，需加上 "best-effort cleanup, non-critical" |

---

## 8. 变更规模

| 维度 | 评价 |
|------|------|
| 行数 | **过大** — 12,182 行跨 170 个文件 |
| Commit 数 | 9 个 commit 可接受 |
| 可审查性 | 多个 commit 相互依赖（t1603 修复 + traversal 重构） |

最大单 commit (`76d0edb`) 混合了 traversal 拆分、多项目 UI 改动和 adapter 改动。理想情况应拆成 3 个独立 commit：(1) traversal 拆分，(2) T1603 adapter，(3) 前端 composables。但 commit message 描述清晰，意图明确。

---

## 9. 未暂存变更

`.claude/settings.local.json` 有本地修改（向 `permissions.allow` 中添加了 grep 命令和 `npx vue-tsc`）。这是本地开发配置变更——**不要提交。**

---

## 10. Pre-submit 验证清单

```
[ ] cd projects\wind-daq\services\api-go && go test ./internal/... ./api/...
[ ] cd projects\daq-t1603\apps\desktop-wails && go test ./...
[ ] cd projects\wind-daq\apps\desktop-wails\frontend && npm run typecheck
[ ] cd projects\wind-daq\apps\desktop-wails\frontend && npm run build
[ ] cd projects\daq-p1604\apps\desktop-wails\frontend && npm run typecheck
[ ] powershell -File .\scripts\validate-import-direction.ps1
[ ] powershell -File .\scripts\validate-structure.ps1
[ ] T1603 设备实机验证强制二进制模式后数据完整性
```

---

## 11. 裁决

**Approve（有条件）** — Critical 问题已修复，当前状态可合并。建议：

1. **合并前**：修复 `api/motionApi.ts` 中 8 处静默空 catch 块（至少加 `console.error` 日志）
2. **合并后跟进**：
   - 给 `traversal_checkpoint.go:120` 的 `_ = store.Remove(path)` 加注释
   - `scanvalve/scanvalve.go` 中文错误消息改为英文
   - 给 T1603 预存数据竞态提单独 issue
   - `traversalStore.ts` 和 `useTraversalSimulation.ts` 中移除未使用的 import

---

## 12. Review Checklist

### Context
- [x] 理解变更内容和意图

### Correctness
- [x] 变更符合需求/spec
- [x] 边界情况已处理
- [x] Critical 问题已修复（ResumeFromCheckpoint 资源锁）
- [x] 测试覆盖变更逻辑

### Readability
- [x] 命名清晰一致
- [x] 逻辑直观
- [x] 无过度复杂

### Architecture
- [x] 遵循现有模式
- [x] 无过度耦合
- [x] 抽象层次恰当

### Security
- [x] 无 secrets 泄露
- [x] 输入验证到位
- [x] 无注入漏洞

### Performance
- [x] 无 N+1 模式
- [x] 无无限操作
- [x] 插值热路径显著优化

### Verdict
- [x] **Approve** — 可以合并（建议修复 motionApi 空 catch 后）
