# shared

Shared workspace libraries.

- `algorithms/`: reusable algorithm code
- `device-sdk/`: reusable hardware communication and simulation helpers
- `contracts/`: shared API and protocol contracts
- `frontend/`: shared frontend modules and UI assets
