# Five-Hole Interpolator v0.2.1

Release date: 2026-06-29
Package: five-hole-interpolator-0.2.1-amd64-installer.exe

## Summary

规范化生产构建标签，确保所有对外交付 exe 均使用 -tags production 构建。

## Internal

- Taskfile.yml `build-go` 增加 `-tags production -trimpath -buildvcs=false -ldflags="-w -s -H windowsgui"`。
- 新增 `clean` 和 `release` 任务，确保打包前清理旧产物。

## Install / Upgrade

- 如果安装过 v0.2.0，可直接覆盖安装。
- 安装步骤：
  1. 关闭正在运行的旧版。
  2. 运行 five-hole-interpolator-0.2.1-amd64-installer.exe。
  3. 启动后确认 GUI 正常出现。

## Verification

- `go test ./...`: passed
- `npm run typecheck`: passed
- `npm run build`: passed
- `task release`: passed
- `makensis` 构建安装包: passed

## Known Issues

- 暂无。

## Rollback

可直接回退到 v0.2.0，兼容。
