# Wind-DAQ Claude Rules

Single source of truth: `../../CLAUDE.md` and `../../AGENTS.md`.

## Project Addendum

No project-specific overrides currently.

Add local constraints here only when Wind-DAQ behavior differs from workspace defaults.
