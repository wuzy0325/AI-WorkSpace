# DAQ-P-1604 v0.4.0-win7.1 (Windows 7 LTS)

Release date: 2026-07-25
Package: DAQ-P-1604-Win7-Setup-0.4.0-win7.1-x64.exe
SHA-256: 待构建后归档

## Summary

主版本号与 master 0.4.0 对齐的 Win7 LTS 维护版本。本次仅同步 master 上 daq-p1604 相关的 1 个提交（acd21c0：补齐 `IsConnResetByPeer` 13 条单元测试），主体功能（`IsConnResetByPeer` 函数 + `p1604_adapter.go` 的硬/软错误处理）已在 0.3.0-win7.1（d586dec）引入。

## Synced Commits

| Commit | Subject | 备注 |
|---|---|---|
| `acd21c0` | fix(daq-p1604): 识别 u01101/v01101 EOF 并清理已死连接 | 仅 `conn_helpers_test.go` 新增测试；`p1604_adapter.go` 改动已在 d586dec 预实现，cherry-pick 时保留 HEAD |

## User-visible Changes

- 与 0.3.0-win7.1 功能等价，无新增用户可见功能。
- 安装包版本号升级到 `0.4.0-win7.1`，与 master 0.4.0 主版本号对齐，便于跨分支追溯。

## Compatibility

- 配置文件格式：与 0.3.0-win7.1 完全兼容。
- CSV 文件名格式：与 0.3.0-win7.1 一致（`<prefix>-<deviceSlug>-YYYYMMDD-HHMMSS-NNN.csv`）。
- 操作系统：仅支持 Windows 7 SP1 x64、Windows 8.1 x64、Windows 10/11 x64。**不支持 Windows XP/Vista**。
- 与 0.3.0-win7.1 安装包可覆盖升级。

## Install / Upgrade

- 支持覆盖安装，无需先卸载旧版本。
- 推荐步骤：
  1. 关闭正在运行的旧版本。
  2. 运行 `DAQ-P-1604-Win7-Setup-0.4.0-win7.1-x64.exe`。
  3. 启动后确认 GUI 正常出现，设备列表能加载。
  4. （可选）实机验证：连接 DAQ-P-1604，确认单位同步、采集、CSV 录制正常。
- 安装目录默认 `%LOCALAPPDATA%\Programs\daq-p1604-win7\`。

## Verification

- `go build ./...`（GOWORK=off，Go 1.20.14）：passed
- `go vet ./...`：passed
- `go test ./...`：passed（含 `TestIsConnResetByPeer` 13 用例、`TestApplyConfig_V01101EOFTriggersConnectionLost` 等）
- `npm run typecheck`：passed
- `npm run build`：passed
- `npm run build:backend`：passed
- `npm run dist:win7`：passed（产物 NSIS x64 安装包）

## Known Issues

- 360 安全卫士主动防御（ZhuDongFangYu）可能锁定 `app.asar` 文件，导致重复打包时 `win-unpacked` 目录无法清理。建议在 360 信任区添加项目目录，或改用 `--config.directories.output=dist2` 绕过。
- Electron 22.3.27 不支持 `color-mix()` CSS 函数，UI 渲染与 master（Wails v3/Edge）可能有细微差异，已用 rgba fallback 规避。
- DAQ-P-1604 设备固件时间戳 bug 仍存在（fractional 字段以 ~4348Hz 递增），CSV 时间戳已统一截断到秒级规避。

## Rollback

- 可回退到 0.3.0-win7.1（功能等价，仅测试覆盖差异）。配置文件格式兼容；CSV 文件名格式一致。
- 卸载 0.4.0-win7.1 后安装 0.3.0-win7.1 即可，两者注册表项独立。
