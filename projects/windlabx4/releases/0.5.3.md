# Wind-DAQ v0.5.3

Release date: 2026-07-13
Package: wind-daq-0.5.3-amd64-installer.exe

## Summary

遍历活动索引重构 + 采集逻辑微调 + 新增测试覆盖。

## Internal

- TraversalActiveIndex 接口简化，清理冗余代码。
- traversal_acquisition.go 微调。
- 新增 traversal_acquisition_test.go。

## Compatibility

- 配置文件格式：兼容。
- 数据文件格式：兼容。
- 设备协议行为：无变化。

## Install / Upgrade

- 支持覆盖安装，无需先卸载旧版本。

## Verification

- `go test ./...`
- `npm run typecheck`
- `npm run build`
- `go build -tags production -trimpath -buildvcs=false -ldflags="-w -s -H windowsgui"`
- `makensis` 构建安装包

## Known Issues

- DAQ-P-1604 设备固件时间戳 bug 仍存在，CSV 时间戳已统一截断到秒级规避。

## Rollback

- 可直接回退到 0.5.2，配置兼容。
