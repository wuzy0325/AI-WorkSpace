# Wind-DAQ v0.12.4

Release date: 2026-08-05
Package: wind-daq-0.12.4-amd64-installer.exe

## Summary

本版本共享算法包新增三孔探针气流速度 Velocity 输出，为三孔探针风洞验证提供"速度误差范围"验收对象。Wind-DAQ 自有代码无变更，仅因共享算法包扩展重新打包。

## User-visible Changes

- 三孔探针插值算法结果新增 `Velocity` 字段（m/s），由马赫数与大气温度经 `V = Ma · sqrt(γ · R · T_K)` 推导（R=287 J/(kg·K)，γ 复用 calcGamma 温度修正）。
- 业务背景：三孔探针风洞验证需要"速度误差范围"作为验收指标，原先算法只输出 Ma，调用方需各自离线换算 V，跨实现一致性无法保证。
- 兜底语义：Velocity 严格跟随 MachNumber——Ma 有效或兜底（initMa/currentMa）时给出对应速度，Ma 为 0/NaN（输入非法、calcMach 失败）时返回 0。
- 5 个返回点全部填充 Velocity，与现有 MachNumber 行为对齐。
- 数值示例：Ma=0.8 / Tatm=20℃ → V≈274.55 m/s；Tatm=-40℃ → V≈245.90 m/s。

## Added

- **三孔插值算法输出 Velocity**（对应 `shared/algorithms/go/threehole/interpolation`）：
  - `InterpolationResult` 新增 `Velocity float64` 字段（JSON tag `velocity`）。
  - 新增 `calcVelocity(ma, tatm)` helper + `airGasConstant=287` 常量。
  - 5 个返回点全部填充 Velocity（finalizeSingle 3 点 + calculateMulti 4 点 + 入口校验 3 点）。
- 小程序端口（`projects/probe-interpolator-miniprogram/utils/algorithms/threehole/three-hole.js`）同步输出 velocity，跨实现验证链闭环。
- Go 算法包新增 4 个单元/集成测试：`TestCalcVelocity`、`TestCalcVelocity_TemperatureEffect`、`TestCalculate_VelocityOutput`（单文件路径）、`TestCalculate_VelocityOutput_MultiFile`（多文件路径）。

## Compatibility

- 配置文件格式：兼容。
- 数据文件格式：兼容（wind-daq 校准 CSV 暂未新增 Velocity 列，未来接入时再扩展）。
- API 契约：兼容（`InterpolationResult` 新增字段为可选，旧调用方无需改动）。
- 设备协议行为：不变。

## Install / Upgrade

- 可直接覆盖安装 0.12.3，无需卸载或清理用户配置。
- 安装前先关闭正在运行的旧版本 Wind-DAQ。
- 运行 `wind-daq-0.12.4-amd64-installer.exe`，安装后启动应用确认主窗口正常出现、设备扫描与采集功能可用。

## Verification

- `go vet ./...`（shared/algorithms/go/threehole）: passed
- `go test ./...`（shared/algorithms/go/threehole，含新增 4 个测试）: passed
- `go vet ./...` + `go test ./internal/... ./api/...`（wind-daq services/api-go）: passed
- `node verify_three.js`（50 用例 Go↔JS 跨实现数值一致，容差 1e-9）: passed
- `node verify_csv.js / verify_units.js / verify_share_card.js`（103 项断言）: passed
- `task release`: passed
- `makensis '-DARG_WAILS_AMD64_BINARY=..\..\bin\wind-daq.exe' project.nsi`（在 `build/windows/installer/` 目录下执行）: passed

## Artifact

- 归档路径：`projects/wind-daq/releases/bin/wind-daq-0.12.4-amd64-installer.exe`
- 大小：6,962,702 bytes（6.64 MB）
- SHA-256：`5CC4EAAFEB8BAE4E08905B1380ABEF51B75681D4D1E8367DA05310338385036D`
- ProductVersion：`0.12.4`
- Authenticode：未签名

## Known Issues

- 安装包未进行 Authenticode 数字签名，Windows 可能显示"未知发布者"提示，可点击"仍要运行"继续安装。
- wind-daq 前端校准 CSV 暂未接入 Velocity 列输出，仍由前端独立计算；后续接入时统一来源。
- 内嵌 exe 的 ProductVersion 资源为空是 wails3 v3.0.0-alpha2.106 的既有行为（不渲染 `{{.Info.ProductVersion}}` 模板，与 0.12.3 一致），installer 壳 ProductVersion 为 `0.12.4`，对外交付不受影响。

## Rollback

- 可直接回退到 0.12.3：通过"控制面板 → 程序和功能"卸载 0.12.4，再运行 `wind-daq-0.12.3-amd64-installer.exe`。
- 配置与数据文件回退前后兼容，无需额外迁移。
- 回退后 Velocity 字段消失（0.12.3 算法包未输出），需要 V 的调用方需各自离线换算。
