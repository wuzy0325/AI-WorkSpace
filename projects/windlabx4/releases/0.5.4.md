# Wind-DAQ v0.5.4

Release date: 2026-07-13
Package: wind-daq-0.5.4-amd64-installer.exe

## Summary

遍历采集采样循环日志增强：区分"设备无数据"与"通道不匹配"失败分类。

## Internal

- collectAveragedSamples 增加 noDataCount / channelMismatchCount 日志计数。

## Compatibility

- 配置文件格式：兼容。
- 数据文件格式：兼容。
- 设备协议行为：无变化。

## Install / Upgrade

- 支持覆盖安装，无需先卸载旧版本。

## Verification

- `go test ./...`
- `npm run typecheck`
- `npm run build`
- `go build -tags production -trimpath -buildvcs=false -ldflags="-w -s -H windowsgui"`
- `makensis` 构建安装包

## Known Issues

- DAQ-P-1604 设备固件时间戳 bug 仍存在，CSV 时间戳已统一截断到秒级规避。

## Rollback

- 可直接回退到 0.5.3，配置兼容。
